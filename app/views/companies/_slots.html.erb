<% if t.booking_id.nil?  %>
  <p><%= link_to t.start_time.strftime('%I:%M %p'), new_company_booking_path( {company_id: t.tradesman.company_id, timeslot_id: t.id} ), class: 'btn btn-success', remote: true %></p>
<% end %>
